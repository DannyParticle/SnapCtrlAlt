# 更新日志

作者：mimo、DeepSeek Harness 与 DannyParticle。许可：MIT。

本项目语义化版本号大致对齐：破坏性变更升主版本，功能升次版本，修复升修订号。

## 1.1.1 — 2026-09-22

- 修：拖动 / 调整选区时阴影与高亮面板滞后。去掉误加的 90 ms 节流，改为每帧精确更新。
- 修：整屏底图在值未变时仍触发全屏重绘（约 18 ms/帧）；增加变更缓存后不再发生。
- 优：工具栏位置未变时跳过重排；选区未变时不再重建面板像素。
- 优：标注态下在选区外按下可直接重新框选（原先只能 Esc 全退）。
- 测：新增 create / resize / move / annotate 四路径帧耗时脚本与边界像素采样脚本。

## 1.1.0 — 2026-09-22

- 换：工具栏图标改为自绘线性图标（原先的 unicode 字符在缺字体时会变成方框）。
- 加：工具按钮 / 颜色 / 线宽悬停时显示中文功能提示。
- 优：拖框帧耗时从约 38 ms 降到约 1–3 ms（亮屏 / 压暗屏缓存 + 分块更新）。

## 1.0.0 — 2026-09-22

首个可用版本。

- 全局热键 `Ctrl+Alt+D` 唤起截图，`Ctrl+Alt+Shift+Q` 退出。
- 框选后可拖手柄调整、整体平移，双击选整屏；另附 8× 放大镜与色值读数。
- 标注工具含矩形、椭圆、箭头、画笔、文字、马赛克、高斯模糊、取色；另有撤销 / 清空 / 复制 / 另存为。
- 系统托盘常驻与开机自启。
- QQ / TIM 运行时优先转发其截图，失败自动回落本地截图。
- 免安装 exe 与安装版（免 UAC）。
- 全局热键注册失败改为可见提示（原先在 windowed exe 下静默失效）。
